import glob
import subprocess
from setuptools import setup, find_packages, Extension


setup(
    name='picam360',
    version='0.0.1',
    description='An open-source panorama camera',
    packages=find_packages(),
    install_requires=[
    ],
    package_data={},
)
